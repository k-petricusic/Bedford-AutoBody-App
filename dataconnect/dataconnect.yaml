specVersion: "v1beta"
serviceId: "Bedford-AutoBody-App"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Bedford-AutoBody-App-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
